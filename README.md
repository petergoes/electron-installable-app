# Electron installable app

This project is an experiment to see if I can create an electron app that has 
an installer for windows

## Usage:

* Install: `$ npm install`;
* Start: `$ npm start`;
* Create installer: `$ npm run deploy`;
